---
phase: 01-foundation-jobs
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pool-app/src/app/page.tsx
  - pool-app/src/components/job-list.tsx
  - pool-app/src/components/job-card.tsx
  - pool-app/src/components/create-job-form.tsx
  - pool-app/src/components/status-badge.tsx
  - pool-app/src/lib/actions/jobs.ts
  - pool-app/src/app/jobs/[id]/page.tsx
autonomous: true
requirements:
  - JOBS-01
  - JOBS-02
  - JOBS-03

must_haves:
  truths:
    - "Worker can tap New Job, enter a name and/or job number, and see the new job appear in the list"
    - "Worker can see all jobs sorted with drafts pinned to top, then most recent first"
    - "Each job shows a Draft or Submitted status badge"
    - "Worker can tap a job card and navigate to a detail page showing job metadata"
    - "Creating a job with neither name nor job number shows a validation error"
    - "All buttons and inputs meet 48px/56px touch target minimums"
  artifacts:
    - path: "pool-app/src/lib/actions/jobs.ts"
      provides: "createJob Server Action with Zod validation"
      exports: ["createJob"]
    - path: "pool-app/src/components/create-job-form.tsx"
      provides: "Inline expandable form for creating jobs"
      exports: ["CreateJobForm"]
    - path: "pool-app/src/components/job-list.tsx"
      provides: "Server component rendering sorted job cards"
      exports: ["JobList"]
    - path: "pool-app/src/components/job-card.tsx"
      provides: "Single job card with name, number, status, timestamp"
      exports: ["JobCard"]
    - path: "pool-app/src/components/status-badge.tsx"
      provides: "Draft/Submitted badge component"
      exports: ["StatusBadge"]
    - path: "pool-app/src/app/page.tsx"
      provides: "Home page composing CreateJobForm + JobList"
      exports: ["default"]
    - path: "pool-app/src/app/jobs/[id]/page.tsx"
      provides: "Job detail page showing metadata"
      exports: ["default"]
  key_links:
    - from: "pool-app/src/components/create-job-form.tsx"
      to: "pool-app/src/lib/actions/jobs.ts"
      via: "useActionState(createJob, null)"
      pattern: "useActionState.*createJob"
    - from: "pool-app/src/lib/actions/jobs.ts"
      to: "pool-app/src/lib/db.ts"
      via: "db.job.create"
      pattern: "db\\.job\\.create"
    - from: "pool-app/src/lib/actions/jobs.ts"
      to: "pool-app/src/lib/validations/job.ts"
      via: "createJobSchema.safeParse"
      pattern: "createJobSchema\\.safeParse"
    - from: "pool-app/src/app/page.tsx"
      to: "pool-app/src/lib/db.ts"
      via: "db.job.findMany for job list"
      pattern: "db\\.job\\.findMany"
    - from: "pool-app/src/components/job-card.tsx"
      to: "pool-app/src/app/jobs/[id]/page.tsx"
      via: "Link href to job detail route"
      pattern: "href=.*jobs/"
---

<objective>
Build the complete job management UI: job list with create form, job cards with status badges, and job detail page. This is the core user-facing functionality of Phase 1.

Purpose: Workers need to create jobs and select them from a list. This plan delivers the primary user workflow: open app, see jobs, create a new job, tap into a job detail. All iPad sizing constraints from Plan 01's CSS tokens are applied here.

Output: Working job list page with inline job creation, sorted job display with status badges, and job detail view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-jobs/01-CONTEXT.md
@.planning/phases/01-foundation-jobs/01-RESEARCH.md
@.planning/phases/01-foundation-jobs/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan uses directly. -->

From pool-app/src/lib/db.ts:
```typescript
export const db: PrismaClient;
```

From pool-app/src/lib/validations/job.ts:
```typescript
import { z } from "zod";
export const createJobSchema: z.ZodEffects<z.ZodObject<{
  name: z.ZodOptional<z.ZodString>;
  jobNumber: z.ZodOptional<z.ZodString>;
}>>;
```

From pool-app/src/types/index.ts:
```typescript
export type JobStatus = "DRAFT" | "SUBMITTED";
export type CreateJobInput = { name?: string; jobNumber?: string };
```

From pool-app/prisma/schema.prisma:
```prisma
enum JobStatus { DRAFT  SUBMITTED }
model Job {
  id          String    @id @default(cuid())
  name        String?
  jobNumber   String?   @map("job_number")
  status      JobStatus @default(DRAFT)
  submittedBy String?   @map("submitted_by")
  submittedAt DateTime? @map("submitted_at")
  photos      Json      @default("[]")
  formData    Json?     @map("form_data")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")
}
```

From pool-app/src/app/globals.css (iPad tokens):
```css
--spacing-touch: 48px;
--spacing-touch-lg: 56px;
--font-size-body: 18px;
```

shadcn/ui components available: Button, Card, CardContent, Input, Badge, Separator
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Server Action, job components, and home page</name>
  <files>
    pool-app/src/lib/actions/jobs.ts,
    pool-app/src/components/create-job-form.tsx,
    pool-app/src/components/status-badge.tsx,
    pool-app/src/components/job-card.tsx,
    pool-app/src/components/job-list.tsx,
    pool-app/src/app/page.tsx
  </files>
  <read_first>
    pool-app/src/lib/db.ts (Prisma client export name and import path),
    pool-app/src/lib/validations/job.ts (Zod schema export name),
    pool-app/src/app/globals.css (iPad CSS tokens to reference),
    pool-app/src/components/ui/button.tsx (shadcn Button props and variants),
    pool-app/src/components/ui/card.tsx (shadcn Card exports),
    pool-app/src/components/ui/input.tsx (shadcn Input props),
    pool-app/src/components/ui/badge.tsx (shadcn Badge variants),
    .planning/phases/01-foundation-jobs/01-RESEARCH.md (exact component code examples)
  </read_first>
  <action>
1. Create `pool-app/src/lib/actions/jobs.ts`:
   - Add "use server" directive at top
   - Import { db } from "@/lib/db"
   - Import { createJobSchema } from "@/lib/validations/job"
   - Import { revalidatePath } from "next/cache"
   - Export async function createJob(prevState: unknown, formData: FormData) that:
     a. Parses formData with createJobSchema.safeParse({ name: formData.get("name"), jobNumber: formData.get("jobNumber") })
     b. On failure: returns { error: parsed.error.flatten().formErrors.join(", ") }
     c. On success: calls db.job.create({ data: { name: parsed.data.name ?? null, jobNumber: parsed.data.jobNumber ?? null, status: "DRAFT" } })
     d. Calls revalidatePath("/")
     e. Returns { success: true }

2. Create `pool-app/src/components/status-badge.tsx`:
   - Import Badge from "@/components/ui/badge"
   - Export function StatusBadge({ status }: { status: "DRAFT" | "SUBMITTED" })
   - DRAFT: Badge variant="outline" with text "Draft", className="min-h-[32px] text-sm font-medium"
   - SUBMITTED: Badge variant="default" with text "Submitted", className="min-h-[32px] text-sm font-medium bg-green-600"

3. Create `pool-app/src/components/job-card.tsx`:
   - Import Link from "next/link"
   - Import { Card, CardContent } from "@/components/ui/card"
   - Import { StatusBadge } from "@/components/status-badge"
   - Import { formatDistanceToNow } from "date-fns"
   - Import type { Job } from "@prisma/client"
   - Export function JobCard({ job }: { job: Job })
   - Render: Link href={`/jobs/${job.id}`} wrapping a Card with className="min-h-[56px] transition-colors active:bg-zinc-100"
   - CardContent: flex items-center justify-between p-4
   - Left side: job.name || `Job #${job.jobNumber}` in text-lg font-medium text-zinc-900, secondary line showing #{job.jobNumber} if both name and number exist, timestamp via formatDistanceToNow(job.createdAt, { addSuffix: true }) in text-sm text-zinc-500
   - Right side: StatusBadge with job.status

4. Create `pool-app/src/components/job-list.tsx`:
   - Import { JobCard } from "@/components/job-card"
   - Import type { Job } from "@prisma/client"
   - Export function JobList({ jobs }: { jobs: Job[] })
   - If jobs.length === 0: render a centered message "No jobs yet. Create your first job above." in text-zinc-500 text-lg, padded py-12
   - Otherwise: render a div with className="mt-4 space-y-3" mapping jobs to JobCard components with key={job.id}

5. Create `pool-app/src/components/create-job-form.tsx`:
   - Add "use client" directive
   - Import { useActionState, useState } from "react"
   - Import { Button } from "@/components/ui/button"
   - Import { Input } from "@/components/ui/input"
   - Import { Plus, X } from "lucide-react"
   - Import { createJob } from "@/lib/actions/jobs"
   - Import { toast } from "sonner"
   - Export function CreateJobForm()
   - State: isOpen (boolean, default false), useActionState(createJob, null) -> [state, formAction, pending]
   - When !isOpen: render Button onClick={() => setIsOpen(true)} with className="mt-4 w-full min-h-[56px] text-lg", Plus icon + "New Job" text
   - When isOpen: render form with action={formAction}, className="mt-4 space-y-3 rounded-lg border border-zinc-300 bg-zinc-50 p-4"
     - Header row: "New Job" h2 text-lg font-semibold + close Button (ghost variant, min-h-[48px] min-w-[48px], X icon)
     - Input name="name" placeholder="Job name (e.g., Smith Residence)" className="min-h-[48px] text-base"
     - Input name="jobNumber" placeholder="Job number (e.g., 2024-042)" className="min-h-[48px] text-base"
     - Error display: if state?.error, show p with text-base font-medium text-red-600
     - Submit Button type="submit" disabled={pending} className="w-full min-h-[56px] text-lg font-semibold", text "Creating..." when pending else "Create Job"
   - useEffect: when state?.success is true, call toast.success("Job created"), setIsOpen(false)

6. Replace `pool-app/src/app/page.tsx`:
   - This is a Server Component (no "use client")
   - Import { db } from "@/lib/db"
   - Import { JobList } from "@/components/job-list"
   - Import { CreateJobForm } from "@/components/create-job-form"
   - Export default async function HomePage()
   - Fetch jobs: const jobs = await db.job.findMany({ orderBy: [{ status: "asc" }, { createdAt: "desc" }] })
   - Render: main className="mx-auto max-w-2xl px-4 py-6"
     - h1 "Pool Field Forms" className="text-2xl font-bold text-zinc-900"
     - p "Select a job or create a new one." className="mt-1 text-lg text-zinc-600"
     - CreateJobForm component
     - Separator className="my-6"
     - JobList jobs={jobs}

IMPORTANT per CONTEXT.md locked decisions:
- Inline "New Job" button at top of list that expands form fields (no page change)
- Job list sorted most recent first, with draft jobs pinned to top (orderBy status asc puts DRAFT before SUBMITTED alphabetically, then createdAt desc)
- Two status labels: Draft and Submitted
- 48px minimum touch targets on inputs/close button, 56px on primary action buttons (New Job, Create Job)
- 16px+ text-base on all inputs, text-lg on buttons
  </action>
  <verify>
    <automated>cd pool-app && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pool-app/src/lib/actions/jobs.ts contains "use server", "createJob", "db.job.create", "revalidatePath", "createJobSchema.safeParse"
    - pool-app/src/components/create-job-form.tsx contains "use client", "useActionState", "createJob", "min-h-[56px]", "min-h-[48px]"
    - pool-app/src/components/job-card.tsx contains "Link", "href={`/jobs/${job.id}`}", "StatusBadge", "formatDistanceToNow", "min-h-[56px]"
    - pool-app/src/components/job-list.tsx contains "JobCard", "jobs.length === 0"
    - pool-app/src/components/status-badge.tsx contains "Badge", "DRAFT", "SUBMITTED"
    - pool-app/src/app/page.tsx contains "db.job.findMany", "orderBy", "CreateJobForm", "JobList", "max-w-2xl"
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Server Action creates jobs with Zod validation. Home page shows sorted job list with draft pinning, inline create form with expand/collapse, and status badges. All touch targets meet 48px/56px minimums. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create job detail page with metadata display</name>
  <files>
    pool-app/src/app/jobs/[id]/page.tsx
  </files>
  <read_first>
    pool-app/src/lib/db.ts (import path for db),
    pool-app/src/components/status-badge.tsx (StatusBadge import),
    pool-app/src/components/ui/button.tsx (Button import),
    pool-app/src/components/ui/card.tsx (Card exports),
    pool-app/src/components/ui/separator.tsx (Separator import)
  </read_first>
  <action>
1. Create directory structure: pool-app/src/app/jobs/[id]/

2. Create `pool-app/src/app/jobs/[id]/page.tsx`:
   - Server Component (no "use client")
   - Import { db } from "@/lib/db"
   - Import { notFound } from "next/navigation"
   - Import Link from "next/link"
   - Import { Button } from "@/components/ui/button"
   - Import { Card, CardContent } from "@/components/ui/card"
   - Import { StatusBadge } from "@/components/status-badge"
   - Import { Separator } from "@/components/ui/separator"
   - Import { ArrowLeft } from "lucide-react"
   - Import { format } from "date-fns"

   - Export async function generateMetadata({ params }: { params: Promise<{ id: string }> }) that fetches the job and returns { title: `${job.name || "Job #" + job.jobNumber} | Pool Field Forms` }. If job not found, return { title: "Job Not Found" }.

   - Export default async function JobDetailPage({ params }: { params: Promise<{ id: string }> })
     a. Await params to get id: const { id } = await params
     b. Fetch job: const job = await db.job.findUnique({ where: { id } })
     c. If !job: call notFound()
     d. Render: main className="mx-auto max-w-2xl px-4 py-6"
       - Back navigation: Link href="/" with Button variant="ghost" className="min-h-[48px] gap-2 text-base", ArrowLeft icon + "Back to Jobs"
       - Job header section:
         - h1 with job.name || `Job #${job.jobNumber}` in text-2xl font-bold text-zinc-900
         - If both name and jobNumber: show p with "#{job.jobNumber}" in text-lg text-zinc-600
         - StatusBadge with job.status
         - Created date: format(job.createdAt, "PPP 'at' p") in text-base text-zinc-500
         - If job.submittedBy: show "Submitted by: {job.submittedBy}" in text-base text-zinc-600
       - Separator className="my-6"
       - Placeholder sections for future phases:
         - Card with CardContent showing "Photos" heading and "Photo capture coming in Phase 2" in text-zinc-500
         - Card with CardContent showing "Form" heading and "Form fields coming in Phase 3" in text-zinc-500

   Note: In Next.js 16, dynamic route params are a Promise that must be awaited. This is a breaking change from Next.js 14/15. Always use `const { id } = await params`.
  </action>
  <verify>
    <automated>cd pool-app && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pool-app/src/app/jobs/[id]/page.tsx exists
    - File contains "db.job.findUnique", "notFound()", "await params", "StatusBadge", "ArrowLeft"
    - File contains "min-h-[48px]" on the back button
    - File contains placeholder text "Phase 2" and "Phase 3" for future sections
    - File contains generateMetadata function
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Job detail page renders job metadata (name, number, status, created date, submitted by). Back navigation returns to job list. Placeholder cards indicate where Phase 2 photos and Phase 3 forms will go. All touch targets meet iPad minimums. Dynamic params properly awaited for Next.js 16.</done>
</task>

</tasks>

<verification>
- `cd pool-app && npx next build` completes without errors
- Home page renders job list with create form, sorted by status then date
- Job creation Server Action validates with Zod and creates in database
- Job cards link to detail pages
- Job detail page loads individual job metadata with back navigation
- All buttons have min-h-[48px] or min-h-[56px] classes
- All inputs have min-h-[48px] and text-base classes
</verification>

<success_criteria>
- Worker can tap "New Job", enter name/number, and see job appear in list (JOBS-01)
- Worker can tap a job card to navigate to its detail page (JOBS-02)
- Job list shows Draft/Submitted badges, sorted with drafts first (JOBS-03)
- Validation error shown when neither name nor job number is provided
- All interactive elements meet iPad touch target requirements
- App builds and both routes render without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-jobs/01-02-SUMMARY.md`
</output>
