---
phase: 01-foundation-jobs
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - pool-app/vitest.config.ts
  - pool-app/src/__tests__/actions/jobs.test.ts
  - pool-app/src/__tests__/validations/job.test.ts
  - pool-app/prisma/seed.ts
  - pool-app/package.json
autonomous: false
requirements:
  - JOBS-01
  - JOBS-02
  - JOBS-03
  - JOBS-04
  - IPAD-01
  - IPAD-02
  - IPAD-03
  - IPAD-04
  - IPAD-05

must_haves:
  truths:
    - "Vitest runs and all tests pass"
    - "Zod validation rejects empty input (no name, no jobNumber)"
    - "Zod validation accepts name-only, jobNumber-only, and both"
    - "createJob Server Action calls db.job.create with correct data"
    - "Seed data populates the database with realistic test jobs"
    - "Worker visually confirms app works on iPad or iPad-sized viewport"
  artifacts:
    - path: "pool-app/vitest.config.ts"
      provides: "Vitest configuration with React plugin and path aliases"
      contains: "@vitejs/plugin-react"
    - path: "pool-app/src/__tests__/validations/job.test.ts"
      provides: "Zod schema validation tests"
      contains: "createJobSchema"
    - path: "pool-app/src/__tests__/actions/jobs.test.ts"
      provides: "Server Action unit tests with mocked Prisma"
      contains: "createJob"
    - path: "pool-app/prisma/seed.ts"
      provides: "Seed data for development"
      contains: "db.job.create"
  key_links:
    - from: "pool-app/src/__tests__/validations/job.test.ts"
      to: "pool-app/src/lib/validations/job.ts"
      via: "import createJobSchema"
      pattern: "import.*createJobSchema"
    - from: "pool-app/src/__tests__/actions/jobs.test.ts"
      to: "pool-app/src/lib/actions/jobs.ts"
      via: "import createJob"
      pattern: "import.*createJob"
---

<objective>
Add automated tests for job creation logic, seed the database with realistic development data, and verify iPad constraints visually.

Purpose: Tests lock in the job creation behavior so future phases cannot accidentally break it. Seed data makes the app usable during development. The iPad checkpoint catches visual/touch issues that automated tests cannot.

Output: Passing test suite, seeded database, and human-verified iPad experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-jobs/01-CONTEXT.md
@.planning/phases/01-foundation-jobs/01-RESEARCH.md
@.planning/phases/01-foundation-jobs/01-01-SUMMARY.md
@.planning/phases/01-foundation-jobs/01-02-SUMMARY.md

<interfaces>
<!-- Key contracts from prior plans that tests validate against. -->

From pool-app/src/lib/validations/job.ts:
```typescript
import { z } from "zod";
export const createJobSchema: z.ZodEffects<z.ZodObject<{
  name: z.ZodOptional<z.ZodString>;
  jobNumber: z.ZodOptional<z.ZodString>;
}>>;
// Refine: at least one of name or jobNumber must be provided
```

From pool-app/src/lib/actions/jobs.ts:
```typescript
export async function createJob(prevState: unknown, formData: FormData): Promise<{ error?: string; success?: boolean }>;
```

From pool-app/src/lib/db.ts:
```typescript
export const db: PrismaClient;
```

From pool-app/prisma/schema.prisma:
```prisma
enum JobStatus { DRAFT  SUBMITTED }
model Job { id String @id @default(cuid()), name String?, jobNumber String?, status JobStatus @default(DRAFT), ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Vitest, write unit tests, and create seed data</name>
  <files>
    pool-app/vitest.config.ts,
    pool-app/src/__tests__/validations/job.test.ts,
    pool-app/src/__tests__/actions/jobs.test.ts,
    pool-app/prisma/seed.ts,
    pool-app/package.json
  </files>
  <read_first>
    pool-app/tsconfig.json (path aliases for @/ resolution),
    pool-app/package.json (current scripts and deps),
    pool-app/src/lib/validations/job.ts (exact Zod schema to test against),
    pool-app/src/lib/actions/jobs.ts (exact Server Action signature to test),
    pool-app/src/lib/db.ts (Prisma client import to mock)
  </read_first>
  <action>
1. Create `pool-app/vitest.config.ts`:
   ```typescript
   import { defineConfig } from "vitest/config";
   import react from "@vitejs/plugin-react";
   import path from "path";

   export default defineConfig({
     plugins: [react()],
     test: {
       environment: "jsdom",
       globals: true,
       setupFiles: [],
     },
     resolve: {
       alias: {
         "@": path.resolve(__dirname, "./src"),
       },
     },
   });
   ```

2. Create `pool-app/src/__tests__/validations/job.test.ts`:
   - Import { describe, it, expect } from "vitest"
   - Import { createJobSchema } from "@/lib/validations/job"
   - Test cases:
     a. "accepts name only" -- safeParse({ name: "Smith Residence" }) should succeed
     b. "accepts jobNumber only" -- safeParse({ jobNumber: "2024-042" }) should succeed
     c. "accepts both name and jobNumber" -- safeParse({ name: "Smith", jobNumber: "042" }) should succeed
     d. "rejects empty input" -- safeParse({}) should fail
     e. "rejects both undefined" -- safeParse({ name: undefined, jobNumber: undefined }) should fail
     f. "rejects name over 200 chars" -- safeParse({ name: "x".repeat(201) }) should fail
     g. "rejects jobNumber over 50 chars" -- safeParse({ jobNumber: "x".repeat(51) }) should fail
     h. "accepts empty string name with jobNumber" -- safeParse({ name: "", jobNumber: "042" }) should succeed (name is optional, jobNumber satisfies refine)

3. Create `pool-app/src/__tests__/actions/jobs.test.ts`:
   - Import { describe, it, expect, vi, beforeEach } from "vitest"
   - Mock "@/lib/db" module: vi.mock("@/lib/db", () => ({ db: { job: { create: vi.fn().mockResolvedValue({ id: "test-id", status: "DRAFT" }) } } }))
   - Mock "next/cache": vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))
   - Import { createJob } from "@/lib/actions/jobs"
   - Import { db } from "@/lib/db"
   - beforeEach: vi.clearAllMocks()
   - Test cases:
     a. "creates job with name" -- create FormData with name="Smith Residence", call createJob(null, formData), expect db.job.create called with data containing name: "Smith Residence" and status: "DRAFT"
     b. "creates job with jobNumber" -- create FormData with jobNumber="2024-042", call createJob(null, formData), expect db.job.create called with data containing jobNumber: "2024-042"
     c. "returns error when neither name nor jobNumber" -- create empty FormData, call createJob(null, formData), expect result to have error property, expect db.job.create NOT called
     d. "calls revalidatePath on success" -- create FormData with name, call createJob, expect revalidatePath called with "/"
     e. "returns success on valid create" -- call createJob with valid FormData, expect result to have success: true

4. Create `pool-app/prisma/seed.ts`:
   - Import { PrismaClient } from "@prisma/client"
   - const prisma = new PrismaClient()
   - async function main() that creates 6 seed jobs:
     a. { name: "Smith Residence Pool", jobNumber: "2024-042", status: "DRAFT" }
     b. { name: "Johnson Backyard Renovation", jobNumber: "2024-043", status: "DRAFT" }
     c. { name: "Oakwood Community Center", jobNumber: "2024-038", status: "SUBMITTED", submittedBy: "Mike", submittedAt: new Date("2024-03-15") }
     d. { name: "Garcia Family Pool", status: "DRAFT" }
     e. { jobNumber: "2024-041", status: "SUBMITTED", submittedBy: "Carlos", submittedAt: new Date("2024-03-20") }
     f. { name: "Riverside Estates HOA", jobNumber: "2024-044", status: "DRAFT" }
   - Use upsert or deleteMany + createMany to make it idempotent
   - Call prisma.$disconnect() in finally block

5. Update `pool-app/package.json` scripts:
   - Add "test": "vitest run --reporter=verbose"
   - Add "test:watch": "vitest"
   - Add "db:seed": "npx tsx prisma/seed.ts"
   - Add "db:push": "npx prisma db push"
   - Add "db:generate": "npx prisma generate"
   - Verify "prisma" seed config: add to package.json: "prisma": { "seed": "npx tsx prisma/seed.ts" }

6. Install tsx if not present: `npm install -D tsx`

7. Run tests: `cd pool-app && npm test`

8. If DATABASE_URL is set, run seed: `cd pool-app && npx prisma db push && npm run db:seed`
   If DATABASE_URL is NOT set, skip seeding and note in summary that user needs to provision Neon and set DATABASE_URL.
  </action>
  <verify>
    <automated>cd pool-app && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - pool-app/vitest.config.ts contains "vitest/config", "@vitejs/plugin-react", alias "@" resolving to "./src"
    - pool-app/src/__tests__/validations/job.test.ts contains at least 5 test cases including "rejects empty input"
    - pool-app/src/__tests__/actions/jobs.test.ts contains "vi.mock", "db.job.create", "revalidatePath", "FormData"
    - pool-app/prisma/seed.ts contains at least 4 seed jobs with mix of "DRAFT" and "SUBMITTED" statuses
    - pool-app/package.json contains "test": "vitest run", "prisma": { "seed" }
    - `npx vitest run` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Vitest configured with React plugin and path aliases. Zod validation tests cover all edge cases (name only, number only, both, neither, max length). Server Action tests verify Prisma calls and revalidation with mocked dependencies. Seed script ready for database population. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPad visual and functional verification</name>
  <files>none (verification only)</files>
  <read_first>
    pool-app/src/app/globals.css (confirm iPad CSS tokens are present),
    pool-app/src/app/layout.tsx (confirm meta tags are present)
  </read_first>
  <action>
Start the dev server and guide the user through verifying the complete Phase 1 application.

Prerequisites -- if DATABASE_URL is not yet set:
1. User must go to https://console.neon.tech and create a free project
2. Copy the pooled connection string
3. Create pool-app/.env.local with DATABASE_URL="the_connection_string"
4. Run: cd pool-app && npx prisma db push && npm run db:seed

Then start: cd pool-app && npm run dev

Present the following verification checklist to the user:

**Job List (JOBS-02, JOBS-03):**
- Seed jobs appear with Draft/Submitted badges
- Draft jobs appear before submitted jobs
- Jobs sorted most recent first within each status group

**Job Creation (JOBS-01):**
- "New Job" button expands form inline (no page navigation)
- Submitting with both fields empty shows validation error
- Entering a name and tapping "Create Job" adds job to list
- Success toast appears

**Job Detail (JOBS-02):**
- Tapping a job card navigates to /jobs/[id]
- Job name, number, status badge, and created date shown
- "Back to Jobs" returns to list

**iPad Constraints (resize browser to 1024x768 or use actual iPad):**
- IPAD-01: All buttons/inputs visibly large (48px+ height)
- IPAD-02: Tapping input does NOT trigger page zoom (16px+ font)
- IPAD-03: Layout adapts between landscape (1024x768) and portrait (768x1024)
- IPAD-04: All text is high contrast (dark on light), no light gray labels
- IPAD-05: /manifest.webmanifest returns valid JSON in Network tab
  </action>
  <verify>User confirms all checklist items pass or reports specific issues</verify>
  <acceptance_criteria>
    - User confirms job list displays with correct sorting and status badges
    - User confirms inline job creation works with validation
    - User confirms job detail page loads with metadata and back navigation
    - User confirms touch targets are 48px+ on buttons and inputs
    - User confirms input focus does not trigger iOS Safari zoom
    - User confirms layout works in both orientations
    - User confirms high contrast text readability
    - User confirms web manifest is served
  </acceptance_criteria>
  <done>User types "approved" confirming all Phase 1 requirements are met visually and functionally. Any issues reported have been addressed.</done>
</task>

</tasks>

<verification>
- `cd pool-app && npx vitest run --reporter=verbose` passes all tests
- Zod schema rejects empty input, accepts name-only and jobNumber-only
- Server Action tests confirm db.job.create called with correct data
- Visual verification confirms iPad touch targets, font sizes, contrast, and orientation
- Web manifest accessible at /manifest.webmanifest
</verification>

<success_criteria>
- All automated tests pass (Zod validation + Server Action unit tests)
- Seed data populates 6 realistic jobs with mixed statuses
- Human verifies: job creation, job list sorting, job detail navigation
- Human verifies: iPad touch targets (48px+), no iOS zoom on input focus (16px+), orientation support, high contrast
- Human verifies: web manifest present for home screen bookmarking
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-jobs/01-03-SUMMARY.md`
</output>
